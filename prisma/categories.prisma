model Categories {
    id String @id @default(uuid())
    tags String @unique @db.VarChar(80)

    gearCategories GearCategories[]
    createdAt DateTime @default(now())
    updatedAt DateTime @updatedAt

    @@map("categories")
}